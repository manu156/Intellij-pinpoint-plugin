# Qodana configuration:
# https://www.jetbrains.com/help/qodana/qodana-yaml.html

version: 1.0
profile:
  name: qodana.starter
  # todo : change it to qodana.recommended
exclude:
  - name: All
    paths:
      - .qodana
